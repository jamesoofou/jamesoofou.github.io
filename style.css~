/* Simple black background with lime green text */
body {
    background-color: black;
    color: white;
    max-width: 600px;
    margin: 0 auto;
}

a.nunder:hover {
    text-decoration: none;
}

/* Links */
a, a:visited {
  color: #4da6ff;       /* Bright blue to stand out */
  text-decoration: none;
  font-weight: 500;     /* Slightly bolder for distinction */
  transition: color 0.2s;
}

/* Hover/focus for clear interactivity */
a:hover, a:focus {
  color: #80cfff;       /* Lighter blue on hover */
  text-decoration: underline;
}

blockquote {
    border-left: 4px solid LimeGreen;
    padding-left: 12px;           
    margin: 1em 0;                
}

.jhq-container {
    position: relative;
    width: 150px;
    height: 191px;
    display: inline-block 
}

.jhq-container img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    transition: opacity 0.3s ease;
}

.jhq-container img.top {
    opacity: 1;
}

.jhq-container:hover img.top {
    opacity: 0;
}

.door-container {
    position: relative;
    width: 92px;
    height: 128px;
    display: inline-block 
}

.door-container img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    transition: opacity 0.3s ease;
}

.door-container img.top {
    opacity: 1;
}

.door-container:hover img.top {
    opacity: 0;
}

#countdown {
    color: limegreen;
    font-family: Courier, monospace;
    font-size: 32px;
}
